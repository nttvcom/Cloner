/** Footprint of a pressure plate; ButtonDef.position is its top-left corner. */
export const BUTTON_WIDTH = 44;
export const BUTTON_HEIGHT = 10;

/** Thickness of a laser beam, centered on the emitter's origin. */
export const LASER_BEAM_THICKNESS = 6;

/** Size of the emitter box drawn (and solid) at a laser's origin. */
export const LASER_EMITTER_SIZE = 14;
